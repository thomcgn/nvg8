# FILE: src/test/resources/application-test.yml

spring:
  test:
    database:
      replace: none

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

security:
  jwt:
    issuer: "test-issuer"
    secret: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef" # >= 256-bit
    baseTtlMinutes: 60
    accessTtlMinutes: 60